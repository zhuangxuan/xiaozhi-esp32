# 4G 远程遥控车方案

立创实战派 ESP32-S3 做车上的控制器，鸿蒙手机做遥控器，两边都主动连到自建后端，由后端转发油门和转向。板子在 4G 下没有公网地址，所以必须是车和手机都向外连，不能让手机直接连板子。

现有工程里，`xiaozhi-esp32` 的 `lichuang-dev` 就是这块板，固件跑在 ESP-IDF 的 FreeRTOS 上；`RemoteCar` 还是空的鸿蒙页面。小智里直接沿用联网、设备身份、断线重连和 JSON 通道。语音、唤醒词、大模型不走驾驶链路，那条链路延迟高，也不适合连续摇杆。

后续方案文档都放在本目录（`xiaozhi-esp32`）。改动边界见同目录的 `Agent.md`。

## 链路

鸿蒙手机通过 WebSocket 连接后端。后端负责鉴权、绑定、转发和踢人。立创 S3 同样通过 WebSocket 连接后端，只把最新一条驾驶指令交给电机任务。电调或电机驱动再驱动遥控车。

控制指令大约 10 到 20 次每秒。4G 往返常见是 80 到 200 毫秒，板子不追历史指令，只执行序号最大的那一条。超过约 300 毫秒没有新指令，油门和转向回到中位并停车。手机松手也发停车。

## 车上固件

在 `xiaozhi-esp32` 上改，板型是 `lichuang-dev`（`main/boards/lckfb/szpi-esp32s3`）。

保留：WifiBoard 启动、配网、WebSocket 重连、屏幕状态、设备 ID。

新增两个 FreeRTOS 任务：一个收网络指令，一个固定频率刷新 PWM。收不到新指令约 300 毫秒就停车，这项判断放在板子上，不依赖服务器。

代码在 `main/boards/lckfb/szpi-esp32s3/car_link.cc`。油门 PWM 用 GPIO18，转向用 GPIO16，50Hz，中位 1500µs。摄像头这期不初始化。车连接 `ws://<主机>:8090/ws/car`，地址保存在 NVS 命名空间 `car` 的 `url`，缺省是当前局域网测试地址。屏幕显示服务端返回的配对码。

驾驶指令用独立消息，字段固定为：

- 序号
- 时间戳
- 油门，范围 -100 到 100
- 转向，范围 -100 到 100
- 有效期

板子回心跳：在线、信号、电量、当前油门和转向、故障。

小智现有的 MCP 工具调用是一问一答，适合查状态，不适合连续开车。

## 后端

遥控服务做在 `xiaozhi-server-go/car-relay`，入口是 `main.go`，默认监听 `:8090`。它是独立进程，自己的 `main`。它不接小智的语音服务，也不把驾驶消息写进 `src/core/connection_handlemsg.go` 的 `hello`、`listen`、`chat`、`mcp` 分发。

手机和车各一条 WebSocket。职责只有四件：

1. 手机登录
2. 用屏幕上的配对码绑定某辆车
3. 同一辆车同时只允许一个控制者
4. 按序号转发，并丢掉过期指令

车和手机断线时，服务端不再补发最后一条油门。手机断开且车的连接还在时，补发一条停车。真正停车仍以板子上约 300 毫秒收不到新指令为准。

数据库用 SQLite，单独一个文件，不和语音服务的 `config.db` 混用。驾驶指令不入库，只留在内存里转发。第一期只有一个 `car-relay` 进程，不为它加 MySQL 或 Redis。

用 Docker Compose 部署，和现有小智服务放在同一台机器、同一个 Compose 里，分成两个容器，数据卷分开：

| 容器 | 作用 |
|---|---|
| 现有小智服务 | 语音会话，保持原样 |
| `car-relay` | 登录、配对、驾驶权、WebSocket 转发 |

`car-relay` 只监听容器内部端口，对外走 HTTPS / WSS。镜像以非 root 运行，`restart: unless-stopped`。健康检查只表示进程活着，不表示有车在线。日志打到标准输出。板上和手机里配置域名的 `wss://`，不写死 IP。

## 鸿蒙应用

在现有 `RemoteCar` 工程上做。

一个摇杆页面，加上急停、连接状态、延迟和电量。按住才给速度，松手立刻发停车。用 WebSocket，不用 HTTP 轮询。手机连 `ws://<主机>:8090/ws/phone` 或对应的 `wss://`。

手机发出的文本消息：

- `hello`：`role` 为 `phone`
- `pair`：`code` 为屏幕上的配对码
- `drive`：`seq`、`ts`、`throttle`、`steer`、`ttl_ms`（300）
- `estop`：`seq`、`ts`，同时再发一条油门和转向都为 0 的 `drive`
- `ping`：`ts`，只用来算延迟，不驱动车子

服务端回到手机：

- `pair`：`ok`，失败时带 `message`
- `status`：`carOnline`、`battery`、`fault`
- `pong`：带回 `ping` 的 `ts`
- `error`：`message`

## 和现有硬件的衔接

立创实战派本身没有 4G。目前只有板子、遥控车和鸿蒙手机。

第一期让板子连家里的 Wi-Fi 或手机热点，手机可以走蜂窝网。协议和后端与以后上 4G 相同。

第二期加一块小智已经适配过的 Cat.1 模组（例如 ML307）和 SIM 卡。固件改成工程里已有的 `DualNetworkBoard` 接法，驾驶协议不用改。

这块板的屏幕、音频和 I2C 引脚已经被占用。本期不做图传，摄像头那组脚可以改作两路 PWM（油门、转向）以及以后的 4G 串口。电机电流走原来的电调或一块电机驱动板，ESP32 只出 3.3V 信号，和车体电源共地、分开供电。

不要为了这辆车去改小智里其他板型的引脚。

## 实施顺序

1. 板子连 Wi-Fi，后端能看到设备上线，屏幕显示配对码。
2. 鸿蒙页能绑定这台车，并看到心跳。
3. 摇杆驱动两路 PWM，松手和断线都会停车。用舵机或电调在台架上试，先不上路。
4. 确认同一辆车不能被第二台手机同时控制。
5. 再加 4G 模组，只替换网络出口。
6. 图传以后走另一条上行流，不和驾驶指令混在同一个包里。

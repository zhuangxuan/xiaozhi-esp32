# 远程遥控车

立创实战派 ESP32-S3 上车，鸿蒙手机做遥控器。车和手机都主动连接遥控服务，服务只转发当前控制者的最新一条油门和转向。语音继续走小智，驾驶不进语音会话。

本文件是后续改动的边界。方案正文见同目录的 `远程遥控车方案.md`。固件通用约定见 `AGENTS.md`。以后新的方案文档都放在本目录，不要写到 `xiaozhi-esp32` 外面。

## 代码放哪

| 目录 | 职责 |
|---|---|
| 本目录 | 车上固件。板型 `lichuang-dev`，目录 `main/boards/lckfb/szpi-esp32s3` |
| `../xiaozhi-server-go` | 小智语音服务，入口 `src/main.go`。另加独立遥控进程，建议目录 `car-relay/`，自己的 `main` |
| `../RemoteCar` | 鸿蒙遥控应用 |

遥控进程单独监听端口，使用自己的 SQLite 文件，不读写语音服务的 `config.db`。不要把驾驶消息加进 `src/core/connection_handlemsg.go` 的 `hello`、`listen`、`chat`、`mcp` 分发。不要让 `car-relay` 引用语音连接处理器。设备可以用同一块板的 MAC 作为标识，账号、配对码和驾驶权只存在遥控库里。

同一台机器用一个 Compose 跑两个容器：现有小智服务，加上 `car-relay`。数据卷分开。

## 驾驶协议

手机到服务、服务到车，字段保持一致：

- `seq`：递增序号。服务和车都只执行比上一条更大的序号
- `ts`：发出时间
- `throttle`、`steer`：`-100` 到 `100`
- `ttl_ms`：有效期

频率约 10 到 20 次每秒。过期指令丢掉，不缓存，不在服务重启后重放。车离线时直接告诉手机。日志只记上下线、配对、抢控制和被丢弃的序号。

停车以车上为准：大约 300 毫秒没有新指令，油门和转向回中。手机松手发停车。手机断开且车仍在线时，服务补发一条停车。

同一辆车同时只有一个控制者。第二条手机连接只能看状态。

## 固件

保留小智的配网、重连、屏幕和设备 ID。新增两个任务：收网络指令，以及固定频率刷新 PWM。断链停车放在板子上。

连续驾驶走上面的专用消息。MCP 只适合查状态，不用于摇杆。

`lichuang-dev` 没有 4G。第一期板子走 Wi-Fi 或手机热点，连的仍是同一个遥控服务。第二期再加小智已适配的 Cat.1（如 ML307），固件改用已有的 `DualNetworkBoard`，驾驶协议不变。

屏幕、音频和 I2C 引脚保持不动。本期不做图传，摄像头那组脚可改作油门、转向 PWM，以及以后的 4G 串口。ESP32 只出 3.3V 信号。电机电流走原车电调或电机驱动板，与开发板共地、分开供电。

不要为了这辆车去改小智里其他板型的引脚。

## 鸿蒙应用

在 `RemoteCar` 里做。按住才给速度，松手立刻发停车。页面上始终有急停、连接状态、延迟和电量。用 WebSocket，不用 HTTP 轮询。

## 先不要做

- 图传，以及把图像和驾驶指令打进同一个包
- 多个 `car-relay` 副本，以及为此引入 Redis 或 MySQL
- 把遥控服务拆成独立仓库。语音上游开始互相冲突时再拆
